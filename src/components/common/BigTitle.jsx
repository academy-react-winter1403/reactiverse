import React from 'react'

const BigTitle = ({title}) => {
  return (
    <div className='text-[#236485] text-4xl font-bold flex justify-center'>
         {title}
    </div>
  )
}

export default BigTitle
